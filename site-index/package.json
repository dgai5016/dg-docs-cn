{
  "name": "dg-docs-cn-site-index",
  "version": "1.0.0",
  "description": "Root index page for dg-docs-cn, lists all translated projects",
  "private": true,
  "type": "module",
  "scripts": {
    "build:projects": "node .vitepress/scripts/build-projects.mjs",
    "dev": "npm run build:projects && vitepress dev",
    "build": "npm run build:projects && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}
